# read each row of the testing_workload.csv file and generate the corresponding test case (the prams.hjson file) for the gemm mem chip 1 cluster workload. 
# then compile and run the test case and collect the results. 
# finally, save the results to a csv file.
# reuse the code in the testing_workload.py file to generate the testing_workload.csv file.
# reuse the code in test_util.py to compile sw, hw, etd and run the test case i=recuirsively and collect the results. 
