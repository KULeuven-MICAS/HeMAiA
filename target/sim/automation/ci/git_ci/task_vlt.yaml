runs:
  # offload_legacy / single_chip: snax-test-integration
  - HOST_APP_TYPE: offload_legacy
    CHIP_TYPE: single_chip
    WORKLOAD: None
    DEV_APP: snax-test-integration
  # host_only / single_chip: clk_rst_configurator
  - HOST_APP_TYPE: host_only
    CHIP_TYPE: single_chip
    WORKLOAD: clk_rst_configurator
    DEV_APP: None
  # offload_legacy / single_chip: snax-xdma-copy
  - HOST_APP_TYPE: offload_legacy
    CHIP_TYPE: single_chip
    WORKLOAD: None
    DEV_APP: snax-xdma-copy
  # offload_legacy / single_chip: snax-xdma-hazard
  - HOST_APP_TYPE: offload_legacy
    CHIP_TYPE: single_chip
    WORKLOAD: None
    DEV_APP: snax-xdma-hazard
  # offload_bingo_sw / single_chip: dummy_1cluster
  - HOST_APP_TYPE: offload_bingo_sw
    CHIP_TYPE: single_chip
    WORKLOAD: dummy_1cluster
    DEV_APP: snax-bingo-offload
  # offload_bingo_hw / single_chip: attn_test_single_gemm_4cluster
  - HOST_APP_TYPE: offload_bingo_hw
    CHIP_TYPE: single_chip
    WORKLOAD: attn_test_single_gemm_4cluster
    DEV_APP: snax-bingo-offload
  # host_only / single_chip: ci_ara
  - HOST_APP_TYPE: host_only
    CHIP_TYPE: single_chip
    WORKLOAD: ci_ara
    DEV_APP: None
