{
    num_clusters: 4
    // Attention test: K-split=4 GEMM across 4 clusters with
    // int32_add reduction and dequantize.
    //
    // X[seq_len x d_model] @ W[d_model x d_head] with K-dim split
    // across 4 clusters, partial D accumulation via int32_add, then
    // dequantize to FP32.
    array_shape: 0
    seq_len: 64
    d_model: 32
    d_head: 32
    k_split: 4
    transposed_A: 0
    transposed_B: 0
    accumPrevC: 0
}
