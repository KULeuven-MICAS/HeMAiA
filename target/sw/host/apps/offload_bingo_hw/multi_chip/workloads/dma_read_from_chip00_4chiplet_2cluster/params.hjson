{
    num_chiplets: 4
    num_clusters: 2

    // Eight 1 KiB byte-ramp chunks A1..A8 live in the memory chip.
    // Each chiplet uses cluster0 and cluster1 for one chunk each.
    num_data: 8
    data_bytes: 1024
}
